export default class Patcher {
    #private;
    constructor(embeddedName?: string);
    after: (funcName: string, obj: any, patch: Function) => () => void;
    before: (funcName: string, obj: any, patch: Function) => () => void;
    instead: (funcName: string, obj: any, patch: Function) => () => void;
    cleanupAll(): void;
}
